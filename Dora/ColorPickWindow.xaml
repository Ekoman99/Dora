<Window x:Class="Dora.ColorSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dora"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="Color Settings" Height="400" Width="620">
    <Window.Resources>
        <local:HexToColorConverter x:Key="HexToColorConverter"/>
    </Window.Resources>

    <Border CornerRadius="10">
        <Border.Background>
            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                <GradientStop Color="#c2c5c8" Offset="0"/>
                <GradientStop Color="#d7d6d9" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="Graph Color Configuration" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- Colors Grid -->
            <DataGrid x:Name="ColorsGrid" Grid.Row="1" AutoGenerateColumns="False" 
                      CanUserAddRows="False" CanUserDeleteRows="False" Margin="0,10"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Color Element" Binding="{Binding Name}" Width="150" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="5,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Color" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Black" BorderThickness="1" Margin="5,2">
                                    <Rectangle Width="60" Height="25">
                                        <Rectangle.Fill>
                                            <SolidColorBrush Color="{Binding ColorHex, Converter={StaticResource HexToColorConverter}}"/>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Hex Value" Binding="{Binding ColorHex}" Width="100" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontFamily" Value="Consolas"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Change color" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Change Color" Click="ColorPicker_Click" 
                                        Tag="{Binding}" Margin="5,2" Padding="8,5"
                                        Style="{StaticResource IntervalButtonStyle}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Reset" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Reset" Click="ResetSingleColor_Click" 
                                        Tag="{Binding}" Margin="5,2" Padding="8,5"
                                        Style="{StaticResource IntervalButtonStyle}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="SaveButton" Content="Save Changes" Width="100" 
                        Click="SaveButtonClick" Style="{StaticResource IntervalButtonStyle}" Margin="0,0,10,0"/>
                <Button x:Name="ResetButton" Content="Reset All" Width="100" 
                        Click="ResetButtonClick" Style="{StaticResource IntervalButtonStyle}" Margin="0,0,10,0"/>
                <Button x:Name="CancelButton" Content="Cancel" Width="100" 
                        Click="CancelButtonClick" Style="{StaticResource IntervalButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>